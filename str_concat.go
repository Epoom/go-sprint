package sprint

func StrConcat(s1, s2, delim string) string {
	// sets the value of s to equal the string of s1 plus the string of delim plus the string of s2
	s := s1 + delim + s2	
	// then it return the string value of s
	return s
}