package sprint

func IsNegative(n int) bool {
	// set an if condition that checks if the value of n is below 0
	if n < 0 {
		// if the condition is met, returns the boolean value "true"
		return bool(true)
	}
	// if the if condition is NOT met, returns the boolean value "false"
	return bool(false)
}